model User {
    id          Int @id @default(autoincrement())
    email       String @unique
    name        String
    password    String 
    tasks       Task[]
    user_role   UserRole?
    createdAt   DateTime @default(now()) @map("created_at")

    @@map("users")
}